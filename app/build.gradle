plugins {
    id 'io.quarkus'
}

dependencies {
    // REST JSON support
    implementation 'io.quarkus:quarkus-resteasy-jackson'
    implementation project(':interface')
    runtimeOnly project(':runtimeModule')
}